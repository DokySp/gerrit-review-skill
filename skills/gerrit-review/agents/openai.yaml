interface:
  display_name: "Gerrit Review"
  short_description: "Query Gerrit reviews and draft replies"
  default_prompt: "Use $gerrit-review to inspect the current Gerrit patch set and draft replies."
